#ifndef UTILS_H
#define UTILS_H

// temporal delay function, just loop 100,000 times
void delay();
// delay for msec milliseconds
void delay_ms(unsigned int msec);
// delay for usec microseconds
void delay_us(unsigned int usec);

#endif